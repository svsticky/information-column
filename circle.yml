general:
    build_dir: code

machine:
    python:
        version: 3.4.3

test:
    post:
        - pip install pylint; pylint infozuild --errors-only
        - pip install sphinx; make -f docs/infozuild-docs/Makefile html
