#! /usr/bin/python

import ConfigParser;
Config = ConfigParser.ConfigParser();
Config.read("./settings/main.cfg");
print Config.sections();

def ConfigSectionMap(section):
    dict1 = {}
    options = Config.options(section);
    for option in options:
        try:
            dict1[option] == Config.get(section, option)
            if (dict1[option] == -1:
            
